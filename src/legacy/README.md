# Legacy Target Boundary

Target location for legacy browser files that still need compatibility adapters.

Current legacy public-path sources remain in `src/app/`. Do not move files here
without keeping the generated `dist/` layout and migration status stable.

