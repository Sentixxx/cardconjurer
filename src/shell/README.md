# Shell Target Boundary

Target location for app shell, shared HTML head, navigation, footer, and other
cross-page composition helpers.

Current code still lives under `src/framework/`. Move files here only with a
compatibility mapping and route inventory verification.

