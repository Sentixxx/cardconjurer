# Deploy Boundary

This directory holds deploy-time policy and platform-specific release guidance.
It is separate from legacy public paths and is not copied into `dist/` by the
current compatibility build.

Current contents:

- `oss/` - OSS/CDN release policy for the generated `release/*` targets.

