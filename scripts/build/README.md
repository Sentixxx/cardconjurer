# Build Scripts Target Boundary

Target location for build and release generation scripts.

Current scripts remain at `scripts/*.mjs` while the migration is in progress.
Move scripts here only when package commands and documentation are updated in
the same step.

