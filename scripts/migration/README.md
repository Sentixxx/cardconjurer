# Migration Scripts Target Boundary

Target location for import-source, baseline, and migration status commands.

Current migration scripts remain at `scripts/*.mjs` and `scripts/lib/*.mjs`.
This directory exists so future moves can be staged without changing behavior
first.

